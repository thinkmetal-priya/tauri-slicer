//   if (
//       (v1.y < planeValue && v2.y > planeValue) ||
//       (v1.y > planeValue && v2.y < planeValue)
//     ) {
//       const t = (planeValue - v1.y) / (v2.y - v1.y);
//       return new THREE.Vector3(
//         v1.x + t * (v2.x - v1.x),
//         planeValue,
//         v1.z + t * (v2.z - v1.z)
//       );
//     }
//   }
// function (p1.x)
